export interface Driver {
  id: string;
  driversName: string;
  firstName: string;
  lastName: string;
  PhoneNumber?: string;
  EmailAddress?: string;
  driverpng?: string;
  // add other fields as needed
}
